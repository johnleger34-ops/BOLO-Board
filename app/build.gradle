plugins {
    id "com.android.application"
}

android {
    namespace "com.boloboard.app"
    compileSdk 35

    defaultConfig {
        applicationId "com.boloboard.app"
        minSdk 26
        targetSdk 35
        versionCode 3
        versionName "1.2.0"
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
}
