plugins {
    id "com.android.application"
}

android {
    namespace "com.boloboard.app"
    compileSdk 35

    defaultConfig {
        applicationId "com.boloboard.app"
        minSdk 26
        targetSdk 35
        versionCode 11
        versionName "1.7.3"
    }

    buildTypes {
        release {
            minifyEnabled false
            signingConfig signingConfigs.debug
        }
    }
}

dependencies {
    implementation 'androidx.core:core:1.13.1'
}
